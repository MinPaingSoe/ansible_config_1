---

- name: ios config command
  hosts: router


  tasks:
    - name: Execute show command
      ios_command:
        commands:
          - interface lo2
      
#      register: show_output
#
#    - debug:
#        var: show_output
